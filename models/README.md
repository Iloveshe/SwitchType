# Models

Place local ASR model files here when running real benchmark engines.

The repository does not commit model binaries. Configure exact paths in `bench/config/benchmark.example.json` or a local copy of that file.

